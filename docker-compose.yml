version: '3.5'
services:
  torchserve:
    build: .
    ports:
     - "0.0.0.0:8080:8080"
     - "0.0.0.0:8081:8081"
    command: torchserve --start --model-store model_store --models cifar10-resnet50=cifar10-resnet50.mar --ts-config properties/config.properties
    # volumes:
    #  - ./app:/app